pishoo {
    # Optional PID file path.
    pid /var/run/pishoo.pid;

    include mime.types;
    default_type application/octet-stream;

    # Pishoo discovers each identity's server.conf through configured workers.
    # Linux packages use the dhttp group; macOS packages use _www.
}
