pishoo {
    # TODO client 证书
    server {
        listen 127.0.0.1:5379;

        location / {
            # DNS 解析服务
            resolver        1.12.74.4:5300;
            proxy_pass      https://$http_host$request_uri;
        }
    }
}