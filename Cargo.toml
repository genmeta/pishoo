[package]
name = "gateway"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1"
tokio = { version = "1", features = ["full"] }
tracing = { version = "0.1", default-features = false, features = [
    "std",
    "attributes",
] }
tracing-subscriber = { version = "0.3", default-features = false, features = [
    "env-filter",
    "fmt",
    "ansi",
    "time",
    "local-time",
] }
bytes = "1.8"

h3 = "0.0.6"
http = "1"
rustls = { version = "0.23.15", default-features = false, features = ["ring"] }
rustls-webpki = "0.102.8"
rustls-native-certs = "0.8"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
dashmap = "6"

gm-quic = "0.0.1"
h3-shim = "0.0.1"
qinterface = "0.0.1"
qconnection = "0.0.1"
qtraversal = "0.0.1"

misc-conf = "0.1"
futures = "0.3"
hyper = { version = "1", features = ["http1", "server", "client"] }

regex = "1"
http-body-util = "0.1"
pin-project-lite = "0.2"

[patch.crates-io]
h3-shim = { git = "ssh://git@github.com/genmeta/gm-quic-traversal.git", branch = "fix/ack" }
gm-quic = { git = "ssh://git@github.com/genmeta/gm-quic-traversal.git", branch = "fix/ack" }
qinterface = { git = "ssh://git@github.com/genmeta/gm-quic-traversal.git", branch = "fix/ack" }
qconnection = { git = "ssh://git@github.com/genmeta/gm-quic-traversal.git", branch = "fix/ack" }
qtraversal = { git = "ssh://git@github.com/genmeta/gm-quic-traversal.git", branch = "fix/ack" }

[patch.'ssh://git@github.com/genmeta/gm-quic-traversal.git']
nat = { git = "ssh://git@github.com/genmeta/mnet.git" }

[profile.dev]
panic = "abort"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = "symbols"
