//! Root process configuration parsing and validation.
//!
//! Delegates to the existing [`crate::config`] module which handles
//! pishoo-specific configuration blocks (worker users, local servers, etc.).

pub use crate::config::*;
