pishoo {
    # Optional PID file path; defaults to /var/run/pishoo.pid
    pid /var/run/pishoo.pid;

    # When no workers are specified, pishoo automatically enumerates
    # all members of the "pishoo" system group as workers.
}