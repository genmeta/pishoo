pishoo {
    # Optional PID file path; defaults to /var/run/pishoo.pid
    pid /var/run/pishoo.pid;

    # Worker usernames to supervise.
    # Each worker runs under its own system user and handles per-server configuration
    # from ~/.genmeta/<identity>/pishoo.conf.
    workers alice bob;
}